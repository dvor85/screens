Listen 9559
<VirtualHost *:9559>

	ServerName ${SITE}
	ServerAdmin webmaster@localhost
	DocumentRoot "${HTML_DIR}"
	Options -Indexes
	
	#UserDir disabled root
	<Directory />
		Require all denied
		Order Deny,Allow
		Deny from All
	</Directory>

	<Directory "${HTML_DIR}">
		#Require all granted
		AuthType Digest
		AuthName "${NAME}"
		AuthUserFile "${PASSWORD_FILE}"
		Require valid-user

		AllowOverride None
		Order allow,deny
		allow from all
	</Directory>

	Alias /archive/ "${ARCHIVE_DIR}/"
	<Directory "${ARCHIVE_DIR}">
		#Require all granted
		AuthType Digest
		AuthName "${NAME}"
		AuthUserFile "${PASSWORD_FILE}"
		Require valid-user

		AllowOverride None
		Order allow,deny
		allow from all
	</Directory>

	Alias /data/ "${DATA_DIR}/"
	<Directory "${DATA_DIR}">
		#Require all granted
		AuthType Digest
		AuthName "${NAME}"
		AuthUserFile "${PASSWORD_FILE}"
		Require valid-user

		AllowOverride None
		Order allow,deny
		allow from all
	</Directory>

	WSGIScriptAlias /api "${CLIENT_API_DIR}/api.py"
	WSGIPassAuthorization On
	<Directory "${CLIENT_API_DIR}">
		#Require all granted
		AuthType Digest
		AuthName "${NAME}"
		AuthUserFile "${PASSWORD_FILE}"
		Require user client

		AllowOverride None
		Order allow,deny
		Allow from all
	</Directory>
	
	WSGIScriptAlias /web "${WEB_API_DIR}/api.py"
	WSGIPassAuthorization On
	<Directory "${WEB_API_DIR}">
		#Require all granted
		AuthType Digest
		AuthName "${NAME}"
		AuthUserFile "${PASSWORD_FILE}"
		Require valid-user

		AllowOverride None
		Order allow,deny
		Allow from all
	</Directory>

	# Possible values include: debug, info, notice, warn, error, crit,
	LogLevel notice

	ErrorLog "${APACHE_LOG_DIR}/error_spserver.log"
	TransferLog "${APACHE_LOG_DIR}/access_spserver.log"
	#CustomLog {logs_dir}/access_kmotion.log combined

</VirtualHost>
